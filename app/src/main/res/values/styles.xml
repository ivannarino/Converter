<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@color/backgroundColor</item>
        <item name="materialButtonStyle">@style/AppButton</item>

        <item name="fontFamily">@font/huge_agb_v5</item>
        <item name="android:fontFamily">@font/huge_agb_v5</item>
    </style>

    <style name="AppButton" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="backgroundTint">@android:color/black</item>
        <item name="cornerRadius">0dp</item>
        <item name="rippleColor">?attr/colorPrimary</item>
    </style>

</resources>
